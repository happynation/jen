pipeline {
    agent any

    stages {
        stage('ssh to ansible server') {
            steps {
                echo 'ssh ansible@192.168.1.96'
            }
        }
        stage('Check the version of Ansible') {
            steps {
                script {
                    variable = 'vova'
                }
                echo "${variable}"

            }
        }
        }
    }
